# ============================================================
# YBCO-TA 统一依赖清单（版本号唯一来源是根目录 VERSION 文件，勿在此硬编码）
# 覆盖 Auto_Sweep + Data_process + Noisesweep 三模块
# 安装：pip install -r requirements.txt
# ============================================================

# ---- 数值 / 科学计算 ----
numpy
scipy

# ---- 绘图 / 微波 ----
matplotlib
scikit-rf            # skrf：S2P 读取与微波网络（Auto_Sweep + Data_process）
h5py                 # HDF5 精扫/噪声数据存储（Noisesweep）

# ---- 仪器驱动 ----
pyvisa>=1.13         # VISA 后端：需本机装 NI-VISA / Keysight IO 的 visa32.dll
nidaqmx              # NI PXIe-4480 DAQ：需本机装 NI-DAQmx 运行时驱动

# ---- GUI ----
PyQt5

# ---- 核心拟合库（scraps：GitHub Actions 自动构建发布的 wheel 直链，Teskiel/scarp）----
scraps @ https://github.com/Teskiel/scarp/releases/download/v0.1.0/scraps-0.1.0-py3-none-any.whl

# ---- 测试 ----
pytest
